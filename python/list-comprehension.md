- List comprehension allows user to create a new list using an existing list in a shorter syntax.
- Without list comprehension we should use a `for` loop to iterate through the list to store.

```
newlist = [_expression_ for _item_ in _iterable_ if _condition_ == True]
```